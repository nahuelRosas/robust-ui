{
  "name": "@robust-ui/contexts",
  "description": "React contexts for Robust UI",
  "version": "0.2.0",
  "author": "Nahuel Rosas <nahuel.rosas@mi.unc.edu.ar>",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup-node src/index.ts* --format esm,cjs --dts --minify terser --external react",
    "dev": "tsup src/index.ts* --format esm,cjs --watch --dts --external react",
    "lint": "eslint \"src/**/*.ts*\"",
    "clean": "rm -rf .turbo node_modules dist"
  },
  "dependencies": {
    "@robust-ui/theme": "workspace:*",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@robust-ui/tsconfig": "workspace:*",
    "@types/react": "^18.2.21",
    "@types/react-dom": "^18.2.7",
    "eslint": "^8.49.0",
    "eslint-config-robust": "workspace:*",
    "tsup": "^7.2.0",
    "typescript": "5.2.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
