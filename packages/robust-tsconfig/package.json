{
  "name": "@robust-ui/tsconfig",
  "description": "Robust TypeScript configuration for Node.js and Browser projects",
  "version": "0.1.0",
  "author": "Nahuel Rosas <nahuel.rosas@mi.unc.edu.ar>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist && rm -rf packages && rm -rf CHANGELOG.md"
  },
  "publishConfig": {
    "access": "public"
  }
}
